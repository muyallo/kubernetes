apiVersion: v1
kind: Service
metadata:
  name: node-web-service
  labels:
    app: node-web
spec:
  type: NodePort
  selector:
    app: node-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000