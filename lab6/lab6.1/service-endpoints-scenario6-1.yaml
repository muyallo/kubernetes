apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: lab6
spec:
  type: ClusterIP
  selector:
    app: backend-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
